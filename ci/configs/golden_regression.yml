# Golden regression scenarios for Smart Student Allocation
# Update paths to point to committed golden Excel inputs and outputs.

base_dir: docs/golden

scenarios:
  - name: sample_allocation
    description: Placeholder scenario using golden Inspactor/School/Crosswalk inputs.
    commands:
      - name: build-matrix (placeholder)
        args:
          - build-matrix
          - --inspactor
          - docs/golden/sample_inspactor.xlsx
          - --schools
          - docs/golden/sample_school_report.xlsx
          - --crosswalk
          - docs/golden/sample_crosswalk.xlsx
          - --output
          - docs/golden/output/sample_matrix.xlsx
        requires:
          - sample_inspactor.xlsx
          - sample_school_report.xlsx
          - sample_crosswalk.xlsx
