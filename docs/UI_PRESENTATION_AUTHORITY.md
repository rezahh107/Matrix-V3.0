# Matrix UI Presentation Authority

## 1. Scope and precedence

This file specifies visual ownership boundaries for the materially used Qt/PySide6 presentation families in Matrix3. It is presentation-only and is subordinate to `AGENTS.md`, `docs/LAW_Smart_Student_Allocation_v3.0.md`, `docs/Technical_SSoT_Smart_Student_Allocation_v3.0-TECH.md`, and the other repository domain authorities. It does not change domain, task, persistence, CLI, Core, or Infra behavior.

This is a specification, not executable drift prevention. Runtime cleanup and invariant tests belong to later phases.

## 2. Native / Styled / Hybrid definitions

- **STYLED** — the Matrix presentation system owns the visible surface and every geometry-sharing visual subcontrol needed for a coherent component. Qt retains behavior, input, accessibility, model/view, focus mechanics, and other widget semantics.
- **NATIVE** — the active Qt/platform style owns the component's visible geometry. Matrix may supply content and non-conflicting application palette/typography, but must not partially repaint geometry-bearing subcontrols.
- **HYBRID** — ownership is explicitly partitioned along a stable boundary that does not give one geometry-sharing surface two incompatible owners. The registry states both sides and the forbidden crossing.

Native behavior is not native appearance. A control may remain behaviorally native while its presentation is Matrix-owned.

## 3. Global ownership invariants

1. **A single geometry-sharing control must not have two incompatible visual owners.**
2. If Matrix styles a control's outer geometry, any subcontrol that interrupts or shares that geometry is Matrix-owned too, unless the registry defines a stable HYBRID boundary.
3. A self-contained native indicator may remain Qt-owned when Matrix does not draw a competing shell around that indicator.
4. `Fusion` is the configured Qt base style. `QPalette` supplies theme-compatible native colors; `styles.qss` is the centralized Matrix styling surface. No custom `QStyle`/`QProxyStyle` is a presentation authority at this repository identity.
5. Local `setStyleSheet()` or `setFont()` calls do not create a new ownership model. They must remain within the registry boundary and must not establish a second geometry owner.
6. LTR and RTL use the same ownership classification. A STYLED geometry-bearing subcontrol must be specified in a direction-safe way rather than by one-direction offsets.

## 4. Control-family boundary registry

| control_family | classification | Matrix_owned_surface | Qt_owned_surface | allowed_customization | forbidden_customization | reason | evidence_paths |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `QMainWindow` / `QDialog` | HYBRID | Client-area background and child presentation through theme palette/QSS. | Native/OS non-client frame, title bar, resize/move/window semantics. | Client-area theme integration. | Custom non-client chrome or a second title-bar/frame owner without reclassification. | Client and non-client geometry are separable. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/history_dialog.py`; `app/ui/mentor_pool_dialog.py` |
| `QLabel` | STYLED | Typography, text color, semantic role/state styling, badge/pill surfaces where assigned. | Text/layout/accessibility behavior. | Token/QSS roles, palette/font integration. | Ad-hoc visual rules that bypass an existing semantic role for the same surface. | No geometry-bearing native subcontrol competes with the styled label surface. | `app/ui/styles.qss`; `app/ui/widgets/database_status_widget.py`; `app/ui/widgets/health_indicator.py`; `app/ui/widgets/status_bar.py` |
| `QLineEdit` | STYLED | Frame, background, border, radius, content padding, text/selection, hover/focus/disabled presentation. | Editing, cursor, selection behavior, accessibility. | Central QSS/token styling and direction-safe content alignment. | Native frame/button geometry reintroduced inside a Matrix-owned edit shell. | Current input shell is already centrally styled and has no required external visual subcontrol. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/widgets/file_picker.py`; `app/ui/mentor_pool_dialog.py` |
| `QPlainTextEdit` | STYLED | Edit frame/viewport, text, selection and log-specific surface. | Editing/scrolling behavior; child scrollbars follow the separate `QScrollBar` authority. | Central QSS and log semantic variant. | Styling only pieces of an embedded scrollbar under this row. | Text surface and scrollbar are separable child control families. | `app/ui/styles.qss`; `app/ui/log_panel.py`; `app/ui/main_window_base.py` |
| `QComboBox` | STYLED | Outer frame, content area, editable-line integration, drop-down button, arrow, popup/view surface, hover/focus/disabled states, and LTR/RTL placement. | Model/view behavior, popup lifecycle, selection, keyboard/mouse behavior, editable semantics and accessibility. | One token/QSS contract covering the complete visible complex control while preserving Qt behavior. | **Matrix-styled outer frame with Fusion-native `::drop-down` or `::down-arrow`;** manual `restore`/behavior substitutes; hard-coded LTR-only arrow geometry. | Drop-down and arrow share/intersect the outer control geometry; one visual owner is required. The active C2/V2 implementation styles the outer shell, drop-down, arrow and popup under the Matrix QSS contract while leaving Qt interaction/model/accessibility behavior intact. | `app/ui/styles.qss`; `app/ui/theme.py`; `app/ui/main_window_base.py`; `app/ui/main_window.py`; `app/ui/preferences/language_dialog.py`; `tests/ui/test_uiux_method_locks.py` |
| `QCheckBox` | HYBRID | Label typography/color and spacing between label and indicator. | Fusion-native self-contained `::indicator`, checked/unchecked/tri-state glyph and indicator state geometry. | Theme-compatible text/palette integration without drawing an outer checkbox shell. | Partial `QCheckBox::indicator` sizing/shape/glyph overrides while leaving the remainder native; custom outer frame around the native indicator. | The indicator is self-contained and does not interrupt a Matrix-owned outer control frame. | `app/ui/styles.qss`; `app/ui/main_window.py`; `app/ui/main_window_base.py` |
| `QPushButton` | STYLED | Button frame, fill, border, radius, padding, primary/secondary roles and hover/pressed/focus/disabled presentation. | Activation, keyboard, accessibility and signal behavior. | Semantic `variant`/object-name styling through central QSS. | Platform-native button shell inside a Matrix-styled button or per-widget replacement of the shared state model. | The visible button surface is already one Matrix-owned geometry. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/widgets/file_picker.py`; `app/ui/widgets/health_indicator.py`; `app/ui/database_tab.py` |
| `QToolBar` / action-generated `QToolButton` | STYLED | Toolbar surface and generated tool-button background, border, padding and interaction states. | Action dispatch, shortcut handling, toolbar/action semantics. | Central toolbar/tool-button QSS; current text-only action presentation. | Reintroducing unrelated native tool-button chrome inside the styled toolbar; partial menu-arrow styling if menu buttons are later introduced. | Current toolbar is a single Matrix presentation surface whose buttons are generated by Qt but centrally skinned. | `app/ui/styles.qss`; `app/ui/main_window_base.py` |
| `QTabWidget` / `QTabBar` | STYLED | Pane, tab surfaces, border/radius, spacing and selected/hover states. | Tab selection/navigation/focus behavior. | Central pane/tab QSS with direction-safe tab geometry. | Mixing a native tab shell/scroller visual into a custom tab strip without explicit re-adjudication. | Pane and tabs form one visible component and are already centrally styled together. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/history_dialog.py` |
| `QGroupBox` | STYLED | Visible group frame, title placement/padding, border/radius and title typography for current non-checkable usage. | Group/container semantics. | Central QSS for frame and title. | Native frame/title geometry combined with a competing custom border; using a checkable group without first adjudicating its indicator. | Current groups are presentation containers with a single styled frame/title surface. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/main_window.py` |
| `QProgressBar` | STYLED | Track/frame, chunk, border/radius and text presentation. | Range/value/busy behavior and accessibility. | Central track/chunk QSS. | Native chunk/track geometry inside a custom progress shell. | Track and chunk are geometry-sharing surfaces and are already styled as one component. | `app/ui/styles.qss`; `app/ui/main_window_base.py` |
| `QFrame` | HYBRID | Named presentation surfaces such as dashboard/hero/action/database/log frames and their Matrix presentation. | Structural frame-shape instances (`NoFrame`, unskinned `StyledPanel`/separator semantics) that have not been promoted to Matrix presentation surfaces. | Choose one owner per frame instance using its explicit presentation role. | Applying a Matrix border/radius to the same frame instance while retaining a competing visible native `StyledPanel`/box border. | The stable boundary is instance role: a frame is either a Matrix surface or a Qt structural frame, never both visually. | `app/ui/styles.qss`; `app/ui/widgets/dashboard_card.py`; `app/ui/widgets/health_indicator.py`; `app/ui/main_window.py`; `app/ui/log_panel.py` |
| `QSplitter` / `QSplitterHandle` (`AccentSplitter`) | STYLED | Handle width, background/accent states and visible grip. | Splitter sizing, drag behavior, orientation and accessibility semantics. | Matrix handle subclass/palette plus matching QSS within one Matrix owner. | Falling back to an unrelated native handle visual inside the custom handle path. | The repository already replaces the handle presentation while retaining native splitter behavior. | `app/ui/main_window_base.py`; `app/ui/styles.qss`; `tests/ui/test_uiux_method_locks.py` |
| `QStatusBar` (`ThemedStatusBar`) | STYLED | Status-bar client surface, border/background and child label/pill presentation. | Status-bar layout/widget-host behavior. | Central QSS plus palette values from the same Matrix theme; size grip remains disabled. | A second local status-bar skin that conflicts with central QSS. | The class explicitly identifies `styles.qss` as its visual skin. | `app/ui/widgets/status_bar.py`; `app/ui/styles.qss`; `app/ui/main_window_base.py` |
| `QScrollArea` | HYBRID | Matrix may suppress the frame (`NoFrame`), control the containing surface/margins, and paint the `QAbstractScrollArea::corner` surface between two scrollbars — the corner's real Qt host; it does not paint replacement scroll chrome or duplicate the `QScrollBar` row's presentation. | Viewport scrolling, scrollbar placement/reserved extent and scroll interaction. | Frame suppression and scroll policies needed by page composition. | Custom rounded/bordered scroll-area shell that conflicts with scrollbar integration; styling scrollbar pieces under this row instead of the `QScrollBar` row. | Removing container chrome is separable from Qt-owned scrolling behavior; scrollbar appearance belongs to its own control family. | `app/ui/main_window.py`; `app/ui/main_window_base.py`; `app/ui/widgets/dashboard_card.py` |
| `QScrollBar` (including scroll-area/item-view children) | STYLED | Complete visible geometry of both orientations: groove/shell, handle (including minimum handle length and rest/hover/pressed/disabled states), `add-line`, `sub-line`, directional arrow subcontrols, `add-page` and `sub-page`. The corner between two scrollbars is **not** a `QScrollBar` subcontrol: Qt hosts `::corner` on `QAbstractScrollArea`, so it is governed by the `QScrollArea` row below. | Scrolling behavior, range/value, mouse/keyboard interaction, accessibility and scroll-area integration. | Central QSS geometry driven by the shared `scrollbar_thickness` / `scrollbar_handle_min` theme tokens; direction-safe rules that apply equally to LTR and RTL. | Partial subcontrol styling that leaves Fusion arrow buttons or a native groove visible inside the Matrix scrollbar; `QProxyStyle`; replacement scroll widgets; a custom scrolling implementation; hover rules that change the reserved scrollbar extent. | Groove, handle and line/page subcontrols share one geometry, so one visual owner is required. The former NATIVE classification produced Fusion arrow-button chrome that read as foreign inside the Matrix surfaces. | `app/ui/styles.qss`; `app/ui/theme.py`; `app/ui/main_window.py`; `app/ui/widgets/dashboard_card.py`; `app/ui/history_dialog.py`; `tests/ui/test_fluent2_visual_polish_contract.py` |
| `QTableView` / `QTableWidget` / `QHeaderView` | STYLED | Table surface, alternate rows, grid/border, selection colors and header-section surface. | Model/view behavior, item geometry, sorting/selection semantics; child scrollbars follow `QScrollBar`. | Central table/header QSS. | Native table/header chrome mixed into the styled table surface; styling child scrollbar pieces as table subcontrols. | Table and header are already one centralized Matrix visual surface. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/history_dialog.py`; `app/ui/history_metrics.py`; `app/ui/dialogs/qa_dashboard_dialog.py`; `app/ui/dialogs/join_key_validation_dialog.py`; `app/ui/database_manager_dialog.py` |
| `QTreeView` | NATIVE | No targeted tree chrome. | Fusion item/tree/branch/header/check-indicator visual geometry and interaction. | Application palette/typography and model data only. | Partial branch, item-indicator or header skinning without reclassifying the whole tree presentation surface. | Current mentor-pool tree has no dedicated QSS; native ownership is the smallest coherent surface. | `app/ui/mentor_pool_dialog.py`; `app/ui/mentor_pool_model.py`; `app/ui/styles.qss` |
| `QListWidget` | NATIVE | No targeted list chrome. | Fusion list/item/selection visual geometry and interaction. | Application palette/typography and list content. | Partial item/selection/frame skinning without reclassification. | Current history run lists have no dedicated QSS; native ownership is coherent and low-maintenance. | `app/ui/history_dialog.py`; `app/ui/history_metrics_dialog.py`; `app/ui/styles.qss` |
| `QFileDialog` | NATIVE | Dialog title/filter/requested path only; no dedicated visual geometry. | Platform/Qt file-dialog surface, navigation and standard controls. | Use standard `getOpenFileName`/`getSaveFileName` behavior. | `QFileDialog`-specific partial QSS, forced custom internal geometry, or relying on incidental fallback-dialog styling as authority. | The repository uses standard file dialogs and does not request a custom non-native file-dialog skin. | `app/ui/widgets/file_picker.py`; `app/ui/database_tab.py`; `app/ui/main_window_base.py` |
| `QMessageBox` | HYBRID | Application client palette/typography and child `QPushButton` presentation under the button row above. | Standard message layout, icon, roles, ordering and modal semantics. | Standard Qt message APIs; Matrix child-control rules may apply without changing message geometry. | Per-instance `QMessageBox.setStyleSheet()` that creates a separate message/button geometry owner. | Standard message semantics are best left Qt-owned while child controls keep their existing family authority. The former startup-local stylesheet was removed by the C2/V2 presentation implementation and is not an authority exception. | `app/main.py`; `app/ui/widgets/health_indicator.py`; `app/ui/database_tab.py`; `app/ui/main_window_base.py` |
| `QDialogButtonBox` | HYBRID | Visible child `QPushButton` presentation follows the `QPushButton` row. | Standard-button ordering, roles, layout and platform semantics. | Standard button roles and Qt ordering. | Styling the box as a competing button shell or overriding platform button ordering for presentation. | The box is a layout/role authority; each visible child button is a separately governed control. | `app/ui/main_window.py`; `app/ui/main_window_base.py`; `app/ui/mentor_pool_dialog.py`; `app/ui/dialogs/join_key_validation_dialog.py`; `app/ui/dialogs/qa_dashboard_dialog.py` |
| `QToolTip` | STYLED | Tooltip background, border/radius, padding and text colors. | Placement, timing, visibility and accessibility behavior. | Central tooltip QSS. | Native tooltip shell combined with a second local tooltip skin. | Tooltip appearance is explicitly centralized and has no geometry-sharing child control. | `app/ui/styles.qss`; `app/ui/main_window_base.py`; `app/ui/widgets/database_status_widget.py` |

At this repository identity, direct material construction was not found for `QSpinBox`, `QDoubleSpinBox`, `QTextEdit`, `QRadioButton`, `QSlider`, `QMenu`, or `QMenuBar`. Existing dormant selectors (where present) do not make a family authoritative or materially used. A later phase that introduces deliberate use must classify the family before adding bespoke presentation rules.

## 5. Forbidden partial-styling patterns

- Custom outer `QComboBox` border/background/radius while `::drop-down` or `::down-arrow` remains an unrelated Fusion visual owner.
- Styling only one geometry-bearing piece of a complex control: a `QScrollBar` handle without its groove, line and page subcontrols, or a tree branch/indicator or other complex native control.
- Giving a `QFrame` both a visible native frame shape and a competing Matrix border/radius on the same instance.
- Styling `QCheckBox::indicator` piecemeal while the indicator remains classified Qt-native.
- Per-widget `setStyleSheet()` that establishes a second visual contract for a centrally governed family.
- Treating Qt behavior ownership as a reason to leave a geometry-sharing visual subcontrol native inside a STYLED shell.
- Using LTR-only offsets to position a STYLED complex-control subcontrol.

## 6. Consumption rules for later UI work

1. Look up the family here before changing its appearance.
2. For **STYLED**, use the existing Matrix theme/QSS authority and cover every relevant geometry-sharing subcontrol and state; keep Qt behavior intact.
3. For **NATIVE**, do not add targeted geometry QSS. Palette/typography integration is allowed only when it does not create a second visual owner.
4. For **HYBRID**, change only the Matrix-owned side named in the registry. Expanding that boundary requires updating this authority before implementation.
5. A materially introduced Qt family not in the registry requires an authority decision before bespoke styling is added.
6. Dormant QSS selectors are not permission to use a family as styled. Actual family use must agree with this registry.
7. A local stylesheet may refine an already Matrix-owned non-geometry property, but cannot override this file's owner classification.
8. This document specifies ownership; code/tests separately implement and verify conformance.

## 7. Evidence basis and current repository identity

- Repository: `rezahh107/Matrix-V3.0`
- Active implementation branch: `claude/fluent2-ui-visual-polish-0ce9qw` (Fluent-2 neutral visual polish, post PR #25)
- Final exact implementation identity is bound by the PR Head and the exact-Head CI/render/DPI manifests; this document does not hard-code an intermediate commit as runtime authority.
- Repository instructions: `AGENTS.md`
- Composition contract: `docs/UI_DESIGN_CONTRACT.md`
- Central presentation sources: `app/ui/theme.py`, `app/ui/styles.qss`, `app/ui/fonts.py`
- Runtime composition evidence: `app/ui/main_window.py`, `app/ui/main_window_presentation_base.py`, `app/ui/main_window_base.py`, `app/ui/widgets/**`, `app/ui/database_tab.py`, `app/ui/history_dialog.py`, `app/ui/history_metrics.py`, `app/ui/history_metrics_dialog.py`, `app/ui/mentor_pool_dialog.py`, `app/ui/dialogs/**`
- Localization evidence: `resources/translations/ui_texts.json`
- Test/evidence sources: `tests/ui/**`, `tools/render_ui_matrix.py`, `tools/validate_ui_dpi.py`

### Current `QComboBox` evidence

The active C2/V2 bytes conform to the existing `QComboBox = STYLED` classification:

- `app/ui/styles.qss` styles the outer `QComboBox` shell, `QComboBox::drop-down`, `QComboBox::down-arrow`, hover/focus/disabled states, and the popup `QAbstractItemView`.
- `app/ui/theme.py` configures `Fusion` for Qt behavior before applying the Matrix palette and centralized QSS.
- `tests/ui/test_uiux_method_locks.py` proves the Matrix-owned complex-control presentation while retaining Qt model/selection behavior and verifies that targeted `QScrollBar` subcontrol styling remains absent.
- `app/ui/main_window_base.py` materially uses theme-selector and editable manager `QComboBox` controls; `app/ui/preferences/language_dialog.py` also uses `QComboBox`.

Historical note: at the Phase-0 inspected Head `8db5e25ea77295befdfb6c7061e0427a37f47eba`, the implementation was `Matrix outer/popup + Fusion drop-down/arrow`. C2/V2 closes that implementation debt without reclassifying the family.

The Fluent-2 polish pass removes the remaining second geometry calculation: the vector chevron overlay in `app/ui/widgets/combo_chevron.py` no longer asks `QStyle.subControlRect(SC_ComboBoxArrow)` for its rectangle. Overlay geometry and the QSS `QComboBox::drop-down` surface are both derived from the single `Theme.combo_dropdown_width` token, and the overlay is placed by logical layout direction (RTL → leading/left drop-down region, LTR → trailing/right drop-down region). Qt keeps model/view, popup lifecycle, selection, keyboard/mouse behavior and accessibility.

### Current `QScrollBar` evidence

`QScrollBar` was reclassified `NATIVE → STYLED` by the Fluent-2 polish pass. The reclassification is complete rather than partial:

- `app/ui/styles.qss` owns `QScrollBar:vertical`, `QScrollBar:horizontal`, `::handle` for both orientations with rest/hover/pressed/disabled states and a minimum handle length, `::add-line`, `::sub-line`, the four directional arrow subcontrols, `::add-page` and `::sub-page`.
- The adjacent corner surface is authored as `QAbstractScrollArea::corner` in the same central QSS. Qt's style-sheet reference defines `::corner` as "the corner between two scrollbars in a `QAbstractScrollArea`", so `QScrollBar::corner` names no real subcontrol and would never match; the rule is written against its actual host under the `QScrollArea` row.
- `app/ui/theme.py` supplies `scrollbar_thickness` and `scrollbar_handle_min` as shared semantic geometry tokens; the reserved extent does not change between rest and hover, so hovering cannot reflow page content.
- The groove and page subcontrols paint no separate material: they leave the already opaque host surface visible, so the scrollbar never introduces a bright track or a translucency layer.
- No `QProxyStyle`, custom scrolling implementation or replacement scrollbar widget exists; Qt keeps range/value, wheel/keyboard interaction and accessibility.
